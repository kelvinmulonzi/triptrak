// destinations.dart

import 'package:flutter/material.dart';

class DestinationsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Destinations'),
      ),
      body: Center(
        child: Text('This is the Destinations Page'),
      ),
    );
  }
}
